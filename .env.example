# Optional. When set, the founding network and contact forms POST their JSON
# payload to this URL instead of simulating a successful submission locally.
# Point it at your own endpoint or a hosted form service.
#
# VITE_ENQUIRY_ENDPOINT=https://example.invalid/api/enquiries
